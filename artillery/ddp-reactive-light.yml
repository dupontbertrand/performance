config:
  target: http://localhost:3000
  phases:
    - duration: 30
      arrivalRate: 5
      name: DDP light load
  processor: '../tests/ddp-helpers.js'
scenarios:
  - name: Reactive DDP CRUD
    engine: http
    beforeScenario: reactiveCrud
    flow:
      - log: "DDP VU complete"
