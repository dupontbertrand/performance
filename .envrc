# Point to your local Meteor checkout to benchmark specific branches
# Switch between branches (devel, release/3.5, etc.) in this checkout
METEOR_CHECKOUT_PATH='/home/ber/Tech/meteor'

function @tasks-3 {
  ./scripts/monitor.sh tasks-3.x reactive-stress.yml reactive
}
